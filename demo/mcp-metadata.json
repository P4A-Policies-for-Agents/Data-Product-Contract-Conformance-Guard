{
  "protocolVersion": "2025-03-26",
  "transport": { "kind": "streamableHttp", "path": "/mcp" },
  "capabilities": { "tools": { "listChanged": true } },
  "tools": [
    {
      "name": "get_products",
      "description": "Return product-catalog records. Fields mirror the CDGC-governed dim_product.csv columns; the Contract Conformance Guard derives the contract from CDGC and strips/rejects drift. Pass variant=leak or variant=broken.",
      "inputSchema": { "type": "object", "properties": {
        "variant": { "type": "string", "description": "leak | broken" } } },
      "outputSchema": { "type": "object", "properties": {
        "products": { "type": "array" }, "count": { "type": "integer" } } }
    }
  ]
}
